namespace SnakeGame
{
    public enum GameState
    {
        Menu,
        Playing,
        LevelComplete,
        GameOver,
        HighScores
    }

    public enum ElementType
    {
        Food,
        Trap
    }
}